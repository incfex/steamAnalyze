var get_reviews = async (steampowered_api, app_id) => {
  //amount = prompt("How many recent reviews do you want to see?","20");
    //for (offset = 0; offset < amount; offset+=20) {
      return steampowered_api(app_id);
      }

    
  